services:
  - type: web
    name: jeopardy-tournament
    runtime: node
    plan: free
    buildCommand: DATABASE_URL="postgresql://x:x@localhost:5432/x" npm install && npm run build
    startCommand: npx prisma db push --schema=server/prisma/schema.prisma && node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST_PASSCODE
        sync: false
      - key: TOKEN_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 10000
